# Expected packages

Golden outputs for synthetic cases will live here once the report schema stabilizes. Volatile fields such as generation time must be isolated so deterministic evidence content can be compared in tests.

Do not hand-edit generated golden files. Regenerate them with a documented command, review the diff, and record the tool/schema version.
